
server:
  port: 8100

spring:
  application:
    name: spring-petvet-vets-service
  datasource:
    url: jdbc:mysql://localhost:3306/petvet
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop


